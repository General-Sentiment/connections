import { ArenaButton } from "./arena-button";
import Link from "@/components/app-link";
export function Header({ title, titleHref, actions, arenaHref, hideArena = false, showMark = false, back = "/", parent = "Profiles" }: { title?: string; titleHref?: string; actions?: React.ReactNode; arenaHref?: string; hideArena?: boolean; showMark?: boolean; back?: string; parent?: string }) {
  const displayTitle = parent === "Connections" ? title?.replace(/^Connection:\s*/i, "") : title;
  return <div className="page-heading"><h1>{showMark && <><Link href="/" className="header-mark" aria-label="Connections home"><svg viewBox="0 0 54 36" fill="none" stroke="currentColor" strokeWidth="2" aria-hidden="true"><circle cx="17" cy="18" r="14" /><circle cx="37" cy="18" r="14" /></svg></Link><span className="slash" aria-hidden="true">/</span></>}{title ? <><Link className="heading-parent" href={back}>{parent}</Link><span className="slash">/</span><span className="heading-current">{titleHref ? <a href={titleHref} target="_blank" rel="noopener noreferrer">{displayTitle}</a> : displayTitle}</span></> : "Connections"}</h1><div className="page-actions">{actions}{!hideArena && <ArenaButton href={arenaHref} />}</div></div>;
}
